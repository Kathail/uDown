[project]
name = "udown"
version = "0.1.0"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
    "smoke: opt-in tests that hit real network (run with -m smoke)",
]
